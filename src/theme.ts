import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    button: {
      textTransform: "none"
    },
    h1: {
      fontSize: "2.5rem"
    },
    h2: {
      fontSize: "2rem"
    },
    h3: {
      fontSize: "1.7rem"
    },
    h4: {
      fontSize: "1.3rem"
    }
  }
});
